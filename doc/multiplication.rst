Multiplication
--------------

Our multiplication engine will be accessible via
`https://api.calculate.solutions/multiply/<num1>/<num2>`.

We support integers of any size.
Latencies start at 5s,
but can be a little more depending on the size of the integer.
